# Local PostgreSQL container settings. Copy this file to .env before running Docker Compose.
# These are placeholders, not production credentials. Use a unique URL-safe local password.
POSTGRES_DB=stoksync
POSTGRES_USER=stoksync
POSTGRES_PASSWORD=replace-with-a-local-dev-password
POSTGRES_PORT=5432

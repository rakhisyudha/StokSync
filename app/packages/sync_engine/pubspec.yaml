name: sync_engine
description: Framework-independent synchronization orchestration for StokSync.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies: {}

dev_dependencies:
  lints: 6.0.0
  test: 1.25.6
